# Iris Classifier (Decision Tree)

## Overview
End-end ML example from Digital Marketing Mastery Module > builds a decision-tree classifier on the classic Iris data set using scikit-learn.

## Quickstart 
'''bash
git clone https://github.com/abbieanderson57-svg/iris-classifier.git
cd iris-classifier
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
python src/train.py
